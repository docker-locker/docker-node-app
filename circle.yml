machine:
  services:
    - docker

deployment:
  hub:
    branch: master
    commands:
      - docker info
      - docker build -t dockerdojo/node-express .
      - docker images
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push dockerdojo/node-express
