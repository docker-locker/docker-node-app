sudo: required

language: node_js
node_js:
  - "4.1"

services:
  - docker

after_success:
  - docker build -t renjith/node-express .
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push dockerdojo/node-express
